[pytest]
markers =
    unit: pure function tests, no external calls
    integration: fixture-based orchestration tests, no external calls
    smoke: live API tests — never run in CI
addopts = -m "not smoke"
